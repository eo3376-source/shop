# 쇼핑몰 화면 디자인 기준

## 1. 문서의 범위

이 문서는 아래 세 참고 화면에서 직접 확인하거나 픽셀 단위로 잰 시각 규칙만
기록한다.

| 참고 화면 | 원본 크기 | 읽어 온 항목 |
|---|---:|---|
| `references/screens/home.png` | 1106 × 797px | 상품 목록, 4열 상품 배치, 정렬 영역, 상품명과 가격의 위계 |
| `references/screens/detail.png` | 1270 × 831px | 상품 이미지와 정보의 2열 배치, 수량 행과 행동 버튼 |
| `references/screens/cart.png` | 1288 × 847px | 장바구니 목록, 주문 금액 상자, 수량 조절기, 합계의 위계 |

참고 화면의 로고, 브랜드명, 상품 사진과 문구는 가져오지 않는다. 상품 이름,
가격, 설명, 분류와 사진은 `SHOP.md`의 상품 표를 따른다. 상품 사진은
`references/products/`의 8장만 그대로 사용한다.

PNG에는 원본 CSS와 글꼴 정보가 들어 있지 않다. 글꼴 이름과 정확한 CSS 값은
화면에서 복원할 수 없으므로, 화면에서 측정한 범위 안에서 구현값을 별도로
승인받아 정했다.

---

## 2. 화면의 인상과 방향

- 한국 오픈마켓처럼 한 화면에서 상품명, 가격, 옵션과 합계를 빠르게 읽을 수
  있도록 정보 밀도를 유지한다.
- 정보는 왼쪽 정렬하고 제목, 가격, 보조 설명의 차이를 크기와 굵기로 분명하게
  나눈다.
- 흰 바탕, 얇은 회색 선, 작은 모서리를 기본으로 한다.
- 큰 장식 카드, 과도하게 둥근 알약 모양, 반투명 유리 효과, 장식용 그라데이션,
  불필요한 일러스트는 사용하지 않는다.
- 리뷰, 별점, 배송·적립 배지, 필터 사이드바, 메가 메뉴, 프로모션 배너는
  `SHOP.md`에 없는 기능이므로 만들지 않는다. 빠진 자리를 장식으로 채우지 않는다.
- 금액은 원 단위 정수에 천 단위 쉼표를 넣고 뒤에 `원`을 붙인다.

---

## 3. 색상

아래 RGB 값은 세 PNG에서 반복되는 단색 픽셀을 읽은 값이다. 색마다 역할을
하나만 배정한다. 상품 사진 안의 색과 원본 브랜드 로고 색은 디자인 색상으로
보지 않는다.

| 색 | 역할 하나 | 확인한 위치 |
|---|---|---|
| `#FFFFFF` | 흰색 표면 | 세 화면의 본문·카드 내부와 외곽선형 버튼 내부 |
| `#F0F0F0` | 콘텐츠 바깥 구획 배경 | `cart.png` 바깥 바탕과 상품 행 사이 띠 |
| `#FAFAFA` | 정렬 도구 영역 배경 | `home.png` 상품 목록 위 정렬 행 |
| `#F2F4F6` | 비활성 버튼 배경 | `cart.png` 주문 금액 상자의 비활성 버튼 |
| `#E0E3E7` | 구조 구분선 | 세 화면의 카드 외곽선과 행 구분선 |
| `#232B35` | 기본 본문 글자 | `cart.png` 상품 정보와 주문 금액 항목 |
| `#131516` | 제목과 최종 합계 글자 | `cart.png` 장바구니 제목과 합계 금액 |
| `#8A8A8A` | 보조 설명 글자 | `cart.png` 옵션 설명, `detail.png` 원산지 설명 |
| `#ACB5BF` | 비활성 안내 글자 | `cart.png` 비활성 주문 버튼과 미완료 단계 |
| `#4269F6` | 사용자가 조작하는 요소의 활성 상태 | `detail.png` 선택 밑줄과 버튼 선 |
| `#BA2D1B` | 판매 가격 강조 | `home.png`와 `detail.png`의 가격 |

색을 추가해야 할 때는 참고 화면에서 먼저 실제 픽셀 값을 찾아 이 표에 역할과
함께 기록한다. 화면에서 찾을 수 없는 색은 추가하지 않는다.

---

## 4. 글꼴과 글자 위계

### 글꼴

| 항목 | 값 |
|---|---|
| 글꼴 계열 | `Arial, "맑은 고딕", sans-serif` |
| 일반 글자의 숫자형 굵기 | `400` |
| 강조 글자의 숫자형 굵기 | `700` |

참고 화면에서 확인되는 획 굵기가 일정하고 장식이 없는 한글 고딕 형태에 맞춰
위 시스템 글꼴을 사용한다. 외부 웹 글꼴은 불러오지 않는다.

### 글자 크기

다음 CSS 값은 PNG에서 측정한 글자 영역 범위 안에서 승인받아 정한 구현값이다.

| 역할 | CSS `font-size` | `font-weight` | 근거 화면 |
|---|---:|---|---|
| 페이지 대제목 | `40px` | `700` | `cart.png`의 장바구니 제목 |
| 목록 제목 | `24px` | `700` | `home.png`의 식품 제목 |
| 주문 금액 상자 제목 | `20px` | `700` | `cart.png`의 주문 예상 금액 |
| 대표 가격 | `24px` | `700` | `detail.png` 대표 가격, `cart.png` 상품 가격 |
| 상품명과 기본 본문 | `16px` | `400` | `detail.png`, `cart.png` 상품 정보 |
| 보조 설명과 수량 | `14px` | `400` | 세 화면의 설명, 수량, 단위 가격 |
| 헤더와 작은 메뉴 | `12px` | `400` | `home.png`, `detail.png` 상단 메뉴 |

한 역할 안에서는 화면마다 글자 크기를 임의로 바꾸지 않는다. 가격 숫자는 단위
표시보다 굵고 크게 보이게 한다. 링크에 불필요한 밑줄을 넣지 않으며, 삭제처럼
참고 화면에서 밑줄이 확인되는 텍스트 행동에만 밑줄을 쓴다.

---

## 5. 여백과 배치

### 공통

- 주요 콘텐츠는 데스크톱 화면 가운데의 흰 영역에 둔다.
- 제목 아래에서 본문이 시작되고, 내용 덩어리 사이는 작은 간격보다 분명히 큰
  간격을 둔다.
- 세 화면에서 반복해서 보이는 간격은 약 `8px`, `12px`, `16px`, `20px`,
  `24px`, `32px` 단위다. 이는 PNG의 요소 경계 사이를 픽셀로 잰 값이다.
- 상품 정보 안의 가까운 두 줄은 약 `4–8px`, 서로 다른 정보 묶음은 약
  `12–20px` 떨어져 있다.
- 카드 내부 여백은 `cart.png`에서 약 `20px`이다.
- 화면 가장자리와 주요 콘텐츠 사이 여백은 화면마다 다르므로 하나의 고정값으로
  만들지 않는다.

### 상품 목록 — `home.png`

- 왼쪽 참고 사이드바는 약 `170px`, 사이드바와 상품 본문 사이는 약 `18px`이다.
  우리 데이터에는 필터 정보가 없으므로 사이드바는 만들지 않고 상품 본문을
  중심 영역으로 사용한다.
- 참고 상품 본문은 x=`188px` 부근에서 시작한다.
- 상품은 데스크톱에서 4열로 나란히 놓인다.
- 상품 이미지 영역은 같은 크기로 맞추고, 이미지 아래에 상품명과 가격을
  왼쪽 정렬한다.
- 상품명과 가격 사이는 약 `8px` 이내의 가까운 간격을 유지한다.
- 목록 제목 아래에 `전체` · `잡화` · `뷰티` · `신발` · `식품` 분류 필터를
  한 줄로 둔다. 높이는 `detail.png`의 선택 행에서 읽은 `44px`를 사용한다.
- 선택한 분류는 `3px solid #4269F6` 밑줄로 표시한다. 선택하지 않은 분류에는
  밑줄을 두지 않는다. 새 배경색이나 둥근 칩 모양을 만들지 않는다.
- 정렬 영역은 상품 격자 위에 놓이며 높이는 약 `38px`이다. `SHOP.md`에 정렬
  기능이 없으므로 구현 화면에는 만들지 않는다.

### 상품 상세 — `detail.png`

- 데스크톱에서 왼쪽 상품 이미지와 오른쪽 상품 정보를 2열로 놓는다.
- 참고 화면에서 정보 열은 x=`573px` 부근에서 시작한다.
- 이미지와 정보 사이에는 넓은 흰 여백을 둔다.
- 1단계에는 상품명, 설명, 가격만 위에서 아래로 배치한다.
- 2단계에는 가격 아래에 수량 조절기와 장바구니 담기 버튼을 추가한다.
- 상품별 크기, 색상, 묶음 수량과 같은 옵션 행은 만들지 않는다.
- 상품 사진은 1:1 정사각형 영역 안에 두고 `object-fit: contain`으로 잘리지
  않게 맞춘다.

### 장바구니 — `cart.png`

- 주요 콘텐츠는 왼쪽 상품 목록과 오른쪽 주문 예상 금액 상자의 2열이다.
- 참고 화면에서 전체 콘텐츠는 x=`63px`부터 x=`1238px` 부근까지다.
- 왼쪽 목록 너비는 약 `787px`, 오른쪽 금액 상자 너비는 약 `375px`, 두 열
  사이는 약 `13px`이다.
- 페이지 제목과 카드 시작점 사이는 약 `40px`이다.
- 상품 한 행에는 1:1 이미지 영역, 상품 정보, 삭제 행동을 왼쪽부터 배치한다.
- 상품 선택 체크박스는 만들지 않는다. 목록의 모든 상품이 합계와 주문 대상이다.
- 수량 조절기는 상품 정보 아래에 두며 버튼과 값이 한 줄에 놓인다.
- 주문 예상 금액 상자는 오른쪽 위에 고정된 열 안에서 시작하며, 항목·구분선·
  합계·주문 버튼 순이다.

### 주문 완료

- 전용 참고 화면이 없으므로 `cart.png`의 카드와 금액 위계를 재사용한다.
- 페이지 대제목 아래의 한 카드 안에 주문 번호, 주문 금액, 주문한 상품 목록을
  순서대로 표시한다.
- 카드의 선, 모서리, 내부 여백과 글자 크기는 장바구니와 같은 값을 사용한다.
- 주문 진행 단계, 배송 예정 정보와 새 장식은 추가하지 않는다.

---

## 6. 선과 모서리

| 요소 | 선 | 모서리 | 근거 화면 |
|---|---|---:|---|
| 카드와 금액 상자 | `1px solid #E0E3E7` | 약 `6px` | `cart.png` |
| 목록 행 구분선 | `1px solid #E0E3E7` | 없음 | 세 화면의 정보 행 |
| 수량 조절기 | `1px solid #E0E3E7` | 약 `5–6px` | `cart.png` |
| 선택 밑줄 | `3px solid #4269F6` | 없음 | `detail.png`의 선택 옵션 |
| 장바구니 담기·주문하기 | `1px solid #4269F6` | `0px` | `detail.png`의 외곽선형 행동 버튼 |
| 비활성 주문하기 | 선 없음 | 약 `6px` | `cart.png`의 비활성 버튼 |
| 상품 이미지 영역 | 선 없음 | `0px` | 목록·상세 화면 |

- 활성 장바구니 담기·주문하기 버튼은 `#FFFFFF` 배경, `#4269F6` 글자와 선을
  사용한다. 높이는 `cart.png` 주문 버튼에서 읽은 약 `55px`로 한다.
- 비활성 주문하기 버튼은 `#F2F4F6` 배경과 `#ACB5BF` 글자를 사용한다.
- 버튼 글자는 `16px`, `700`을 사용한다.

그림자는 상품 목록, 상세 정보, 장바구니 카드에서 사용하지 않는다. 참고 화면의
메가 메뉴에만 보이는 그림자는 우리 화면에 해당 기능이 없으므로 가져오지 않는다.

---

## 7. 이미지

- `SHOP.md` 상품 표가 지정한 `references/products/`의 8장만 사용한다.
- 인터넷 이미지 주소, 외부 아이콘, 대체 상품 이미지를 사용하지 않는다.
- 상품 사진은 다시 자르거나 줄이지 않는다.
- 목록과 상세 모두 1:1 정사각형 이미지 영역을 사용한다.
- `object-fit: contain`으로 사진 전체가 보이게 하고 남는 공간은 `#FFFFFF`로
  둔다.
- 사진마다 비율이 달라도 카드의 이미지 영역 크기와 상품 정보 시작선은 맞춘다.

---

## 8. 화면별 가져오는 것과 제외하는 것

| 참고 화면 | 가져오는 것 | 제외하는 것과 근거 |
|---|---|---|
| `home.png` | 4열 상품 격자, 이미지·상품명·가격 위계, 촘촘한 정보 배치 | 브랜드 헤더·배너·필터·별점·배송 배지: 우리 브랜드가 아니거나 `SHOP.md`에 기능이 없음 |
| `detail.png` | 이미지/정보 2열, 가격 강조, 수량 조절과 외곽선형 행동 | 원본 상품 옵션·문구·리뷰·구매 인원·배송 배지·우측 미니 장바구니: 우리 데이터나 기능이 없음 |
| `cart.png` | 목록/금액 2열, 상품 행, 수량 조절기, 합계 위계 | 원본 로고·선택 체크박스·주문 단계·품절 수치·배송 예정 문구: 우리 브랜드가 아니거나 데이터가 없음 |

이 차이 외에는 참고 화면의 정돈된 밀도, 왼쪽 정렬, 얇은 선, 작은 모서리와
절제된 색 사용을 따른다.

---

## 9. 현재 디자인 범위 밖의 값

참고 화면 세 장은 데스크톱 화면뿐이므로 현재 디자인 범위도 데스크톱으로
제한한다. 모바일 화면의 중단점과 여백은 모바일 참고 화면을 받기 전까지 만들지
않는다.

3단계의 결제 성공·오류 안내에 새 색이 필요하면 참고 화면에서 확인할 수 없으므로
임의로 추가하지 않고 먼저 확인받는다. 기존 본문색과 구조만으로 표현할 수 있으면
새 색을 만들지 않는다.
